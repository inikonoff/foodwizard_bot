from groq import AsyncGroq
from config import GROQ_API_KEY, GROQ_MODEL, GROQ_MAX_TOKENS
from typing import Dict
import json

client = AsyncGroq(api_key=GROQ_API_KEY)

class GroqService:
    @staticmethod
    async def generate_dishes(products: str, style: str = "обычный") -> str:
        """
        Генерирует блюда с учетом стиля и КУЛИНАРНОЙ ЛОГИКИ.
        """
        prompt = f"""У пользователя есть продукты: {products}.
        Базовые продукты (всегда есть): соль, перец, вода, растительное масло, сахар, мука.

        Твоя роль: Профессиональный Шеф-повар.
        Задача: Предложи 6-8 вариантов блюд в стиле: "{style}".

        🚨 ГЛАВНЫЕ ПРАВИЛА (КУЛИНАРНАЯ ЛОГИКА):
        1. ВКУС ПРЕВЫШЕ ВСЕГО. Не предлагай несъедобные сочетания, даже если пользователь перечислил эти продукты. Все компоненты блюда должны дополнять друг друга, а не перебивать. 
        2. ЗАПРЕЩЕНО смешивать несочетаемые ингредиенты (например: рыба + мясо/сало, молоко + соленые огурцы/селедка, сладкие фрукты + чеснок/лук), если это не является классикой высокой кухни.
        3. СЕГМЕНТАЦИЯ: Если пользователь дал список "{products}", и там есть и рыба, и мясо — предложи одно блюдо с рыбой, а другое с мясом. НЕ СМЕШИВАЙ их в одной тарелке.
        4. Используй не менее 50% перечисленных продуктов, но только тех, которые СОЧЕТАЮТСЯ.
        5. Если для нормального блюда не хватает 1-2 ингредиентов — предложи его или их, но напиши в названии: "Название (докупить: ...)".

        Формат ответа:
        🍽️ Название блюда
        Краткое, лаконичное, аппетитное описание - примерное время.

        В конце добавь: '🎤 Назовите блюдо или добавьте продукты'."""

        response = await client.chat.completions.create(
            model=GROQ_MODEL,
            messages=[{"role": "user", "content": prompt}],
            max_tokens=GROQ_MAX_TOKENS,
            temperature=0.2 # Снизил для большей адекватности
        )
            
            raw = response.choices[0].message.content.strip()
            
            # Парсинг
            if "true" in raw.lower(): return True
            if "false" in raw.lower(): return False
            
            # Если json
            start = raw.find('{')
            end = raw.rfind('}')
            if start != -1 and end != -1:
                data = json.loads(raw[start : end + 1])
                return data.get("valid", False)
            
            return False
        except Exception:
            # Если ошибка парсинга, лучше перестраховаться и вернуть False, 
            # но для UX мягче вернуть True и дать попробовать сгенерировать.
            # В данном случае вернем False, чтобы не генерить бред.
            return False

    @staticmethod
    async def generate_dishes(products: str, style: str = "обычный") -> str:
        prompt = f"""У пользователя есть продукты: {products}.
        Базовые продукты (всегда есть): соль, перец, вода, растительное масло, сахар, мука.

        Твоя роль: Профессиональный Шеф-повар.
        Задача: Предложи 3-5 вариантов блюд в стиле: "{style}".

        🚨 ГЛАВНЫЕ ПРАВИЛА (КУЛИНАРНАЯ ЛОГИКА):
        1. ВКУС ПРЕВЫШЕ ВСЕГО. Не предлагай несъедобные сочетания.
        2. ГАРМОНИЯ ВКУСОВ: Все компоненты блюда должны дополнять друг друга.
        3. ЗАПРЕЩЕНО смешивать несочетаемые ингредиенты (рыба + мясо, молоко + соленые огурцы), если это не классика.
        4. СЕГМЕНТАЦИЯ: Рыба отдельно, мясо отдельно.
        5. Используй ~50% продуктов, но только тех, которые СОЧЕТАЮТСЯ.
        6. Если не хватает ингредиента — пиши в названии: "Название (докупить: ...)".

        Формат ответа:
        🍽️ Название блюда
        Краткое, аппетитное описание - примерное время.

        В конце добавь: '🎤 Назовите блюдо или добавьте продукты'."""

        response = await client.chat.completions.create(
            model=GROQ_MODEL,
            messages=[{"role": "user", "content": prompt}],
            max_tokens=GROQ_MAX_TOKENS,
            temperature=0.5
        )
        return response.choices[0].message.content
    
    @staticmethod
    async def determine_intent(user_message: str, dish_list: str) -> Dict:
        prompt = f"""Контекст (предложенные блюда):
        {dish_list}
        Сообщение пользователя: "{user_message}"
        Задача: определить намерение.
        1. Называет блюдо -> "select_dish"
        2. Добавляет продукты -> "add_products"
        3. Непонятно -> "unclear"
        ВЕРНИ ТОЛЬКО JSON."""

        response = await client.chat.completions.create(
            model=GROQ_MODEL,
            messages=[{"role": "user", "content": prompt}],
            max_tokens=200,
            temperature=0.1
        )
        
        raw_result = response.choices[0].message.content.strip()
        try:
            start_index = raw_result.find('{')
            end_index = raw_result.rfind('}')
            if start_index != -1 and end_index != -1:
                return json.loads(raw_result[start_index : end_index + 1])
            else:
                return {"intent": "unclear"}
        except Exception:
            return {"intent": "unclear"}
            
    @staticmethod
    async def generate_recipe(dish_name: str, products: str) -> str:
        prompt = f"""Напиши подробный рецепт: "{dish_name}".
        Доступные продукты: {products}.
        Задача: РЕАЛИСТИЧНЫЙ и ВКУСНЫЙ рецепт.
        
        Формат:
        🍽️ [Название]
        🛒 Ингредиенты: (Пометь ✅ есть, 🛒 докупить)
        👨‍🍳 Приготовление: (по шагам)"""

        response = await client.chat.completions.create(
            model=GROQ_MODEL,
            messages=[{"role": "user", "content": prompt}],
            max_tokens=GROQ_MAX_TOKENS,
            temperature=0.5
        )
        
        recipe_text = response.choices[0].message.content
        return recipe_text + "\n\n👨‍🍳 <b>Приятного аппетита!</b>"

    @staticmethod
    async def generate_freestyle_recipe(dish_name: str) -> str:
        prompt = f"""Напиши классный рецепт блюда: "{dish_name}".
        Пиши с душой, как шеф-повар."""

        response = await client.chat.completions.create(
            model=GROQ_MODEL,
            messages=[{"role": "user", "content": prompt}],
            max_tokens=GROQ_MAX_TOKENS,
            temperature=0.6
        )
        
        recipe_text = response.choices[0].message.content
        return recipe_text + "\n\n👨‍🍳 <b>Приятного аппетита!</b>"
